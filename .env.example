# Copy to .env and fill in values. Used by docker compose and local dev.
# Database (docker compose sets DATABASE_URL for the app automatically)
DATABASE_URL="postgresql://pixora:pixora@localhost:5432/pixora"

# Auth
JWT_SECRET="your-secret-key"

# App URL (for redirects, Stripe, etc.)
NEXT_PUBLIC_URL="http://localhost:3000"
NEXT_PUBLIC_DOMAIN="localhost:3000"

# Optional: Gmail (contact form, invites)
# GMAIL_USER=
# GMAIL_APP_PASSWORD=

# Optional: Stripe
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# NEXT_PUBLIC_STRIPE_CLIENT_ID=
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=

# Optional: Clerk
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
# CLERK_SECRET_KEY=

# Optional: Pusher
# PUSHER_APP_ID=
# NEXT_PUBLIC_PUSHER_KEY=
# PUSHER_SECRET=
# NEXT_PUBLIC_PUSHER_CLUSTER=
